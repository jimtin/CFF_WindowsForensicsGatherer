# Set the version
version: 2.1

# Set up the job to be run on custom image
jobs:
    build:
      docker:
        - image: jimtin/cff_windowsforensicsgatherer

    steps:
      # Run tests
      - run:
          name: "run tests"
          command: "Get-Process"
